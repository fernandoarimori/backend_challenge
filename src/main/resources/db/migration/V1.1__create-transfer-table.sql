CREATE TABLE tb_transfer(
    id varchar(255) NOT NULL,
    value decimal(10,2),
    buy_client varchar(255) NOT NULL,
    sell_store varchar(255) NOT NULL,
    created varchar(255),
    PRIMARY KEY (id)
);